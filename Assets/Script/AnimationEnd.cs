using UnityEngine;

public class AnimationEnd : MonoBehaviour {

	// Use this for initialization
	void Start ( ) {

	}

	// Update is called once per frame
	void Update ( ) {

	}
	void onAnimeEnd ( ) {
		Destroy( gameObject );
	}
}
